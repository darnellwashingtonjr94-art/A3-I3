name: A3-I3 Engine CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Run Engine Tests
        working-directory: .
        run: |
          pip install -r requirements.txt
          make test

      - name: Build and Push Container
        run: |
          docker build -t credkellar/a3-i3-engine:latest .
